import { InRoomSincePipe } from './in-room-since.pipe';

describe('InRoomSincePipe', () => {
  it('create an instance', () => {
    const pipe = new InRoomSincePipe();
    expect(pipe).toBeTruthy();
  });
});
